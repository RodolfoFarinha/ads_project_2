export class Constants {
  static readonly TIME_FMT = 'HH:mm';
  static readonly DATE_FMT = 'dd/MM/yyyy';
  static readonly DATE_TIME_FMT = `${Constants.DATE_FMT} ${Constants.TIME_FMT}`;
  static readonly DATE_SEVER = 'yyyy-MM-dd';
  static readonly TIME_SEVER = 'HH:mm:ss';
  static readonly DATE_TIME_SERVER = `${Constants.DATE_SEVER}T${Constants.TIME_SEVER}`;
}
